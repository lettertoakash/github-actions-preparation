name: First GitHub Actions Workflow

on:
  push:
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello from GitHub Actions"